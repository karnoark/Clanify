// Extends Jest’s expect functionality with custom matchers for testing React Native components.
import '@testing-library/jest-native/extend-expect';

// Increases the default test timeout from 5 seconds (default) to 10 seconds
jest.setTimeout(10000);